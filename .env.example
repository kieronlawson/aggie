# Aggie secrets — set these in GitHub repository secrets for CI,
# and in a local .env (gitignored) for local runs.
ANTHROPIC_API_KEY=
VOYAGE_API_KEY=
TURBOPUFFER_API_KEY=
FIRECRAWL_API_KEY=
SLACK_BOT_TOKEN=
# Optional: TurboPuffer region (defaults to gcp-us-central1)
TURBOPUFFER_REGION=
